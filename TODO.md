# TODO

- Add RSS feed
- Option to retain aspect ratio in gallery
- Filter by tag
- Add external links to the tags
- Fix pre-existing TypeScript errors in app/api/tags/route.ts and lib/db.ts (Set iteration, needs --downlevelIteration or es2015+ target)
- Replace PHOTOS_DIR/DATA_DIR in .env with host-side paths; have docker-compose.yml use those to configure the volume mounts