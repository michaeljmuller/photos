This is a photo gallery web site.

The project builds and runs in containers.  Other than basic tools like docker and git, everything else needed to build
and run the project are provided in containers.

The project should build and run on a Mac (Apple Silicon/ARM) or on a linux host (Intel).

Never commit or push unless directed to do so explicitly.

Do not add AI attribution ("Co-Authored-By: Claude ...") to commit messages.