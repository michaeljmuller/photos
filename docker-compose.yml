services:
  web:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./photos:/photos
      - ./data:/data
    env_file: .env
    restart: unless-stopped

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    profiles:
      - test
